## Done

- Added `ui/codeless-ui/src/lib/rpc/tauri-ipc-client.workspace-attach.test.ts` that mocks `@tauri-apps/api/core`'s `invoke` and asserts `TauriIpcClient.call` dispatches each of the four workspace-attach methods (`attach_workspace`, `detach_workspace`, `list_workspaces`, `validate_workspace_path`) as `rpc_<method>` with the `{ args }` envelope per the documented IPC contract.
- `npx tsc --noEmit` clean; `npx vitest run` on the new file: 4/4 passing.
- Committed as `72f11e3` on `codeless/workspace-attach-ui`.

## Next

- Stage 3 of 8 (next milestone segment from `DOCS/WORKSPACE-ATTACH.md` M3 — likely the `PathPicker` shell capability and its two injectors, then the Settings → Workspaces tab UI for M4).

## What you need to know

- `TauriIpcClient` already implements the four methods *implicitly* via its generic `call<M extends RpcMethod>` that does `invoke("rpc_${method}", { args })`. So M3b was a wire pin via test, not new code in `tauri-ipc-client.ts` — same shape as M3a's `workspace-attach.test.ts` snapshot.
- `AttachedWorkspace` (generated by specta) is `{ repo_id, repo_name, fs_root, attached_at: UnixMillis, default_runner }` — note the test in M3a (`workspace-attach.test.ts`) references richer fields (`canonical`, `is_dir`, etc.) on `ValidateWorkspacePathResult` that *do* exist, but the `AttachedWorkspace` shape in M3a's wire test is not exercised at runtime so the drift wasn't caught there.
- Committed with plain `git`, not `mani`: there is no `bin/mani` reachable from the isolated job worktree. Push, if required, is a follow-up from the workspace root.

## Open questions

- (none)
