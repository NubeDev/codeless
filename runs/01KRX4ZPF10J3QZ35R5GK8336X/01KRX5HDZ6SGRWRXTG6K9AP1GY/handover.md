## Done

- Recorded resolutions for OQ-WASM-1, OQ-WASM-2, OQ-WASM-4, OQ-WASM-5, OQ-MCP-1, OQ-UI-1, and the v0.1 slot-vocabulary lock in `.codeless/jobs/plugin-substrate-runtimes/SCOPE.md § Open questions` (each carries the chosen answer + one-line *why*).
- Propagated each resolution into the owning plugin doc as a "*Resolved 2026-05-18 (plugin-substrate-runtimes stage 1)*" line: `DOCS/plugins/PLUGIN-WASM.md` (four OQs), `DOCS/plugins/PLUGIN-MCP.md` (OQ-MCP-1), `DOCS/plugins/PLUGIN-UI-FEDERATION.md` (OQ-UI-1 + slot-vocabulary lock note).
- Rewrote `handover.md` so stage 2 has the load-bearing decisions on disk (table placement, wit-bindgen commit-in-tree, hot-reload refuse, caps-only-lower, two MCP dispatch kinds, SDK-pin = slot contract, five-slot v0.1 set) plus the stage-2 worklist from `template.yaml`.
- Committed as `08b24ff` on `codeless/plugin-substrate-runtimes` with the stage-1 title verbatim.

## Next

- Stage 2: scaffold `codeless-plugin-sdk` (mobile-safe) with the rubix-lifted `compile_error!` mutual-exclusion guard, the `ToolBehavior` trait, the `#[derive(Tool)]` macro (schemars-driven), and the `register!` macro stub. The existing `crates/codeless-plugin-notes` keeps compiling unchanged through a thin shim; stage 5 swings it onto the SDK.
- Add the new SDK crate to the mobile-safe column of `DOCS/SCOPE.md § Crate layout` in the same stage-2 commit; verify `cargo check -p codeless-client --target aarch64-apple-ios` and `--target aarch64-linux-android` stay green.
- Every file lifted from rubix gets `// codeless-ported-from: rubix-workspace/<path>@<sha>` on its first commit.

## What you need to know

- Biases in the SCOPE brief all held — none required a doc redesign, so the resolutions are the biases as stated with the *why* spelled out. The WORKFLOW.md halt rule ("a redesign-sized resolution is a signal the bias was wrong") was the explicit check.
- Runtime-adapter table lands in `codeless-tools` (mobile-safe) with `HashMap<ToolId, Box<dyn RuntimeAdapter>>`; concrete adapters live in host-only crates behind Cargo features. The iOS/Android cargo-check matrix is the leak canary.
- `wit-bindgen` output is committed in-tree under `crates/codeless-tool-wit/`, regenerated by a `cargo xtask` task (stage 3 fixes the path); no `build.rs`.
- Fuel/memory/wall-clock caps: `HostPolicy` defaults; `config.toml` `[plugins.<id>]` overrides can only lower; the plugin manifest cannot set them at all. Stage 13's manifest parser must reject those fields.
- MCP v0.1 = exactly two dispatch kinds (`tool_call`, `rest_proxy`); `"mcp_forward"` parses and loads `Failed` with reason `"mcp_forward not yet supported"` — same shape as the process-runtime seam.
- Slot-vocabulary contract is the `@codeless/plugin-ui-sdk` semver major.minor pin; v0.1 ships exactly the five-row table already in `PLUGIN-UI-FEDERATION.md § Slot vocabulary`, now annotated as locked.
- `PLUGIN-PROCESS.md` was not edited — its only OQ reference is to OQ-WASM-3, which stays open beyond v0.1 and is not blocking this job. `PLUGIN-SUBSTRATE.md § Open questions` was not edited because its `OQ-PS-*` set is out of scope per the SCOPE brief.
- The job is on branch `codeless/plugin-substrate-runtimes`; the new commit is `08b24ff`. The branch is **not** pushed — WORKFLOW.md calls for `./bin/mani --config mani.yaml run push --projects codeless` from the workspace root, which has to run outside this worktree. The next session (or the JOB-LOOP driver) is expected to push.

## Open questions

- (none) — every OQ named in the stage-1 todo is resolved; OQ-WASM-3 / OQ-UI-2/3/4 / OQ-MCP-2/3/4 stay documented as future work in their respective plugin docs and do not gate this job.
