[package]
name = "codeless-slack"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "Codeless: Slack control-plane adapter. Host-only; subscribes via the in-process RpcServer once later stages wire the command parser + event-bus surface. Stage 2 lands only the Socket Mode transport and the --enable-slack plumbing."

[lints]
workspace = true

[dependencies]
codeless-adapters-host = { path = "../codeless-adapters-host" }
codeless-types = { path = "../codeless-types" }
futures-util = "0.3"
reqwest = { version = "0.12", default-features = false, features = ["json", "rustls-tls"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "1"
tokio = { version = "1", features = ["sync", "rt", "macros", "time"] }
tokio-tungstenite = { version = "0.24", default-features = false, features = ["connect", "rustls-tls-webpki-roots"] }
tracing = "0.1"
url = "2"

[dev-dependencies]
tempfile = "3"
tokio = { version = "1", features = ["sync", "rt", "macros", "time", "rt-multi-thread"] }
