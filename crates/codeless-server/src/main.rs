// The hosted server binary — axum SSE + REST + WebSocket surface plus
// auth middleware. See the `codeless-server` row of the crate table in
// DOCS/SCOPE.md. Depends on `codeless-runtime` and
// `codeless-adapters-host`. Phase 3 fills this in; for now it is the
// shell that proves the workspace compiles.

fn main() {}
