[package]
name = "codeless-rpc"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "Codeless: transport-agnostic RPC trait and client glue. iOS- and Android-safe."

[lints]
workspace = true

[dev-dependencies]
specta-typescript = "=0.0.10"

[dependencies]
codeless-types = { path = "../codeless-types" }
serde = { version = "1", features = ["derive"] }
async-trait = "0.1"
futures-core = "0.3"
thiserror = "2"
specta = { version = "=2.0.0-rc.23", features = ["derive"] }
