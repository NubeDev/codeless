// SSE + REST + WebSocket client glue — see the `codeless-client` row
// of the crate table in DOCS/SCOPE.md. Used by every shell that talks
// to a hosted core (browser, iOS, Android). iOS- and Android-safe;
// the mobile shell depends on this and `codeless-types` only.
