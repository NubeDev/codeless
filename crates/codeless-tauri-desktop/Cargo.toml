[package]
name = "codeless-tauri-desktop"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "Codeless: Tauri 2 desktop shell. Phase 5 — currently an empty binary that proves the workspace compiles without pulling in tauri yet."

[[bin]]
name = "codeless-tauri-desktop"
path = "src/main.rs"

[lints]
workspace = true
