// The Tauri 2 desktop shell — see the `codeless-tauri-desktop` row of
// the crate table in DOCS/SCOPE.md (Phase 5). Intentionally does NOT
// depend on tauri yet. Adding tauri to a workspace member changes the
// compile profile for everything else; deferring that until Phase 5
// keeps Phase 1/2/3 builds fast.

fn main() {}
